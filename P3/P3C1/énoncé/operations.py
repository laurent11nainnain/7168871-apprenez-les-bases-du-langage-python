def sum(a, b, c):
    return a + b + c

def subtraction(a, b, c):
    return a - b - c

def multiplication(a, b, c):
    return a * b * : c

def division(a, b, c):
    if a != 0:
        return a / b / c
    if b != 0:
         return a / b / c
    if c != 0:
        return a / b / c
    else:
        print("erreur : division par zéro")
        
